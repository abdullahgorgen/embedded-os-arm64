# Kernel Config - arm64 QEMU uyumlu

# Processor Features
CONFIG_ARM64=y
CONFIG_ARM64_VA_BITS_39=y
CONFIG_ARM64_PGTABLE_LEVELS=3
CONFIG_ARM64_CONT_PTE=y

# Kernel Özelikleri
CONFIG_SMP=y
CONFIG_HAVE_SMP=y
CONFIG_NR_CPUS=4

# Filesystems (Monitor app ve RootFS için)
CONFIG_PROC_FS=y
CONFIG_PROC_SYSFS=y
CONFIG_SYSFS=y
CONFIG_EXT4_FS=y
CONFIG_EXT4_USE_FOR_EXT2=y
CONFIG_TMPFS=y

# Device Support (QEMU için)
CONFIG_VIRTIO=y
CONFIG_VIRTIO_BLK=y
CONFIG_VIRTIO_NET=y
CONFIG_VIRTIO_PCI=y

# Serial Console (QEMU Serial)
CONFIG_SERIAL_AMBA=y
CONFIG_SERIAL_AMBA_CONSOLE=y
CONFIG_SERIAL_8250=y
CONFIG_SERIAL_8250_CONSOLE=y

# IPC (Aşama 4 için Pipe/Socket)
CONFIG_UNIX=y
CONFIG_PIPE_INTERNAL_PAGES=16

# Module Support
CONFIG_MODULES=y
CONFIG_MODULE_UNLOAD=y

# Debugging
CONFIG_DEBUG_INFO=y
CONFIG_DEBUG_KERNEL=y
CONFIG_PRINTK=y

# Performance
CONFIG_HZ_100=y
CONFIG_TICK_ONESHOT=y

# Security (Basic)
CONFIG_HAVE_EFFICIENT_UNALIGNED_ACCESS=y
